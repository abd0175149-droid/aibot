import { describe, it, expect } from 'vitest';

/**
 * ⚠️ **ملفٌّ مؤقّتٌ لتمرين البوّابة — يُحذف فور انتهاء التمرين.**
 * غرضُه إثباتُ أنّ بوّابة `deploy.sh` توقف النشر فعلاً على اختبارٍ فاشل،
 * وأنّها توقفه **قبل** أن تُلمس صورةٌ أو حاوية. إن وجدتَه في master فهذا
 * خطأ: احذفه.
 */
describe('تمرين البوّابة', () => {
  it('يفشل عمداً', () => {
    expect('البوّابة يجب أن توقف هذا').toBe('نُشر رغم الفشل');
  });
});
