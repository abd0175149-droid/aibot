-- ════════════════════════════════════════════════════════════════
-- ★★ **علامةُ «أُجيب إلى هنا» — وبلا عمودٍ يحملها كان البوت يردّ مرّتين.**
--
-- رسائلُ الزبون التي تصل **أثناء** توليد الردّ لم تكن تُدمَج: كلُّ واحدةٍ
-- تُنتج مهمّةً جانبيّةً تعمل بالتوازي مع الردّ الجاري، فنداءان أو ثلاثة
-- للنموذج على المحادثة نفسها، كلٌّ يقرأ تاريخاً لا يحوي ردَّ الآخر — فيصل
-- الزبونَ ترحيبان. قِيس هذا التوازي فعلاً: مهمّتان بمعرّفَين مختلفَين على
-- المحادثة نفسها عملتا معاً (‏`MAX_CONCURRENT=2`‏).
--
-- والعلاجُ قفلٌ لكلّ محادثة يجعل الردود متسلسلة، ثمّ **بوّابةٌ لا تردّ بلا
-- جديد**. والبوّابةُ تحتاج علامةً: «إلى أيّ رسالةٍ واردةٍ وصل ردّي؟».
--
-- ⚠️ ولا تكفي المقارنةُ بآخر صادر. الردُّ يُحجَز صفُّه بعد التوليد، فتاريخُه
--    **أحدثُ** من رسالةٍ وصلت في أثنائه. فلو كانت البوّابة «هل وصل واردٌ بعد
--    آخر صادر؟» لسقطت تلك الرسالة صامتةً ولم يُجَب عليها أبداً — أي لاستبدلنا
--    ردّاً مكرّراً برسالةٍ مهجورة، وهذا أسوأ. ولذلك العلامةُ **زمنُ أحدثِ
--    واردٍ رآه المخطِّط**، لا زمنُ الصادر.
--
-- ⚠️ ومتماثِل: النشر يُطبّق كلّ الترحيلات في كلّ مرّة.
-- ════════════════════════════════════════════════════════════════

ALTER TABLE conversations ADD COLUMN IF NOT EXISTS bot_answered_at timestamptz;
