-- الإجراء المنتظِر تأكيد الزبون.
--
-- بلا هذا العمود كان نمط الزرّ نصفَ نمط: الأزرار تُرسَل، وضغط «أكّد» يصل
-- ويُخزَّن في messages.payload ولا يقرأه أحد — فلا `request_quote` تُنفَّذ
-- ولا `confirm_booking`، ثمّ يقول النموذج للزبون «تم تسجيل طلبك» وهو لم
-- يُسجَّل. اكتُشف على زبونٍ حقيقيّ على رقمٍ LIVE.
--
-- ⚠️ متماثِل: النشر يُطبّق كلّ الترحيلات في كلّ مرّة.
ALTER TABLE conversations ADD COLUMN IF NOT EXISTS pending_action jsonb;
