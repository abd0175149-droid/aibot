import { defineConfig } from 'vitest/config';

/**
 * ★ إعدادٌ **منفصلٌ باسمٍ صريح** — ولا `vitest.config.ts` في الجذر.
 *
 *   لا إعدادَ vitest في هذا المستودع اليوم، فـ`npx vitest run` — وهو ما
 *   تُشغّله بوّابةُ النشر و CI — يجمع `**\/*.test.ts` بالافتراض. وإضافةُ
 *   إعدادٍ يُكتشَف تلقائيّاً تُغيّر ما تجمعه تلك البوّابةُ من حيث لا نقصد.
 *
 * ⚠️ واللاحقةُ `.itest.ts` لا `.test.ts`: لو حملت اللاحقةَ العاديّة لجمعها
 *    `npx vitest run` فحاولت بوّابةُ النشر رفعَ حاويات دوكر — وهي تعمل قبل
 *    أن تُلمس أيّ حاوية، وعلى خادمٍ حِملُه سبعة. فالبوّابةُ تتعلّق أو تفشل
 *    على شيءٍ لا علاقةَ له بالشيفرة المدفوعة.
 */
export default defineConfig({
  test: {
    include: ['test-integration/**/*.itest.ts'],
    testTimeout: 240_000,
    hookTimeout: 300_000,
    /* عمليّاتٌ لا خيوط، وملفٌّ واحدٌ في كلّ مرّة: الحزامُ يرفع حاوياتٍ
       بأسماءٍ ثابتة (`aibot-itest`)، فشوطان متوازيان يتنازعانها. */
    pool: 'forks',
    fileParallelism: false,
  },
});
